import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chapalbarua',
  appName: 'International Bridge',
  webDir: 'dist/internationalBridge/browser'
};

export default config;
